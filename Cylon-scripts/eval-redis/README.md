# Run Experiments

### Figure 7
This experiments is to compare the latency of Cylon vs CMM-H in Redis YCSB Workload-C.
1. To run and generate the same plot as Figure 7, run it with:
```bash
ENABLE_PRELOAD=1 RUN_TAG=cylon-1t ./run_and_plot.sh
```

2. You can check the graph result inside `../plots/.`

